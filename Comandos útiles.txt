#Iniciar Docker
    sudo dockerd &> /dev/null &

#Probar POST
    curl -i -XPOST 'http://172.18.0.3:8080/telegraf' --data-binary '{"id": 1, "puls": 60, "tension": 100}'

#Obtener IP del container de telegraf
    sudo docker inspect -f '{{range.NetworkSettings.Networks}}{{.IPAddress}}{{end}}' sac_project_telegraf_1

#Borrar datos
    influx
    drop database influx

